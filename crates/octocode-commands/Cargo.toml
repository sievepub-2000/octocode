[package]
name = "octocode-commands"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
octocode-core = { path = "../octocode-core" }
octocode-runtime = { path = "../octocode-runtime" }
