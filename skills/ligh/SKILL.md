---
name: ligh
description: Placeholder descriptor for the user-referenced "ligh" project. Upstream identity is ambiguous (possibly "light", "lighthouse", "ligh-browser"); registered as descriptor-only pending clarification.
integration: reference-only
status: descriptor-only
---

# ligh

Descriptor-only registration (P9 batch).

## Status

The user-supplied identifier `ligh` is ambiguous. Candidate upstreams:
- `lighthouse` (Google) — web-page auditing.
- `lighter` / `light` / `ligh-cli` — multiple small CLI tools share the name.

## Next step

Clarify which project with the user, then replace this file with a concrete `source` URL and `integration` mode. Until then, the hub lists this skill so the ambiguity is visible rather than silently dropped.
