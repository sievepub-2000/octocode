id = skillhub-registry
transport = http
command = https://skill.xfyun.cn
description = iFlytek SkillHub — enterprise agent skill registry with publish, discover, version, RBAC. Self-hostable via Docker/K8s. For local: make dev-all → http://localhost:8080.
trusted = false
