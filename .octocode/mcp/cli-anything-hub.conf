id = cli-anything-hub
transport = stdio
command = cli-hub serve
description = CLI-Anything Hub — browse, search, install, update, and uninstall CLI harnesses that make any software agent-controllable. Requires: pip install cli-anything-hub.
trusted = true
